<% layout('/layout/boilerplate') %>

<!-- Tabulator CSS -->
<link href="https://unpkg.com/tabulator-tables@5.5.2/dist/css/tabulator.min.css" rel="stylesheet" />

<div class="contain" style="flex-direction: column; padding: 10px; gap: 10px">
  <style>
    .header-action-btn {
      background-color: #f0f0f0;
      color: #044a78;
      padding: 2px 14px;
      font-size: 14px;
      font-weight: 500;
      border: none;
      cursor: pointer;
      border-radius: 0.5rem;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
      transition: all 0.2s ease-in-out;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .header-action-btn:hover {
      background-color: #e4e4e4;
    }
    #adhesive-master-table {
      width: 100%;
      flex: 1 1 auto;
      min-height: 0;
    }
    .dialog-header {
      background: #044A78;
      padding: 18px 22px;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .dialog-header h2 {
      margin: 0;
      font-size: 1.05rem;
      font-weight: 700;
      color: #fff;
    }
    .dialog-close-btn {
      background: none;
      border: none;
      color: #fff;
      font-size: 20px;
      cursor: pointer;
      line-height: 1;
      padding: 0 2px;
      opacity: 0.8;
      transition: opacity 0.15s;
    }
    .dialog-close-btn:hover {
      opacity: 1;
    }
    .dialog-body {
      padding: 24px;
    }
    .dialog-field-label {
      display: block;
      font-size: 13px;
      font-weight: 600;
      color: #475569;
      margin-bottom: 8px;
    }
    .dialog-error {
      color: #ef4444;
      font-size: 12px;
      margin-top: 5px;
      display: none;
    }
    .dialog-actions {
      display: flex;
      justify-content: flex-end;
      gap: 10px;
      margin-top: 20px;
    }
    .dialog-field-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 14px;
      margin-bottom: 16px;
    }

    /* Match Choices.js selects to the .premium-input look used by the rest
       of this dialog's fields. */
    .dialog-body .choices__inner {
      padding: 11px 14px !important;
      border-radius: 10px !important;
      border: 1.5px solid #e2e8f0 !important;
      background-color: #f8fafc !important;
      font-size: 14px !important;
      min-height: 0 !important;
      transition: all 0.2s ease !important;
    }
    .dialog-body .choices__list--single {
      padding: 0 !important;
    }
    .dialog-body .choices.is-focused .choices__inner,
    .dialog-body .choices.is-open .choices__inner {
      border-color: #044a78 !important;
      background-color: #ffffff !important;
      box-shadow: 0 0 0 4px rgba(4, 74, 120, 0.1) !important;
    }
    .dialog-body .choices[data-type*="select-one"]::after {
      display: block !important;
      content: "" !important;
      width: 12px !important;
      height: 8px !important;
      border-style: none !important;
      border-width: 0 !important;
      background-image: url("data:image/svg+xml,%3Csvg width='12' height='8' viewBox='0 0 12 8' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M1 1l5 5 5-5' stroke='%23044a78' stroke-width='2' fill='none'/%3E%3C/svg%3E") !important;
      background-repeat: no-repeat !important;
      background-size: contain !important;
      position: absolute !important;
      right: 14px !important;
      top: 50% !important;
      margin-top: 0 !important;
      transform: translateY(-50%) !important;
    }
    .dialog-body .choices[data-type*="select-one"].is-open::after {
      transform: translateY(-50%) rotate(180deg) !important;
    }
    /* Dropdown list must render above the dialog overlay (see CLAUDE.md
       "Dialog / modal pattern" -- Choices.js in dialogs). */
    .dialog-body .choices__list--dropdown {
      border-radius: 10px !important;
      border: 1.5px solid #e2e8f0 !important;
      z-index: 99999 !important;
    }
  </style>

  <div class="indi-head">
    <h1>Adhesive Master</h1>
    <div style="display: flex; gap: 10px; align-items: center">
      <button class="header-action-btn" onclick="openAddDialog()">
        <i class="fa-solid fa-plus" style="margin-right: 4px"></i> Adhesive
      </button>
    </div>
  </div>

  <div class="table-spinner"><div class="spinner-circle"></div></div>
  <div id="adhesive-master-table" class="tabulator tabulator-modern"></div>
</div>

<!-- ============ ADD ADHESIVE DIALOG ============ -->
<div class="logout-modal" id="addDialog" aria-hidden="true">
  <div class="logout-dialog" role="dialog" aria-modal="true" aria-labelledby="addDialogTitle"
    style="width: min(460px, 95vw); padding: 0; border-radius: 14px;">
    <div class="dialog-header" style="border-radius: 14px 14px 0 0;">
      <h2 id="addDialogTitle">Add Adhesive</h2>
      <button class="dialog-close-btn" type="button" onclick="closeAddDialog()" aria-label="Close">×</button>
    </div>
    <div class="dialog-body" style="border-radius: 0 0 14px 14px;">
      <label class="dialog-field-label" for="addSkuIdPreview">SKU ID</label>
      <input type="text" id="addSkuIdPreview" class="form-control premium-input" readonly style="margin-bottom: 16px; background: #f1f5f9;" />

      <div class="dialog-field-row">
        <div>
          <label class="dialog-field-label" for="addVendorSelect">Vendor Name</label>
          <select id="addVendorSelect">
            <option value="" disabled selected hidden>Select vendor</option>
            <% vendors?.forEach(v => { %>
              <option value="<%= v._id %>"><%= v.vendorName %></option>
            <% }) %>
          </select>
        </div>
        <div>
          <label class="dialog-field-label" for="addTypeSelect">Type</label>
          <select id="addTypeSelect">
            <option value="" disabled selected hidden>Select type</option>
            <option value="ACRYLIC">ACRYLIC</option>
            <option value="HOTMELT">HOTMELT</option>
            <option value="WATERBASE">WATERBASE</option>
          </select>
        </div>
      </div>

      <label class="dialog-field-label" for="addMakeInput">Make</label>
      <input type="text" id="addMakeInput" class="form-control premium-input" placeholder="Enter make" style="margin-bottom: 16px;" />

      <div class="dialog-field-row">
        <div>
          <label class="dialog-field-label" for="addVendorSkuCodeInput">Vendor SKU Code</label>
          <input type="text" id="addVendorSkuCodeInput" class="form-control premium-input" placeholder="Enter vendor SKU code" />
        </div>
        <div>
          <label class="dialog-field-label" for="addShelfLifeInput">Shelf Life</label>
          <input type="text" id="addShelfLifeInput" class="form-control premium-input" placeholder="e.g. 12 Months" />
        </div>
      </div>

      <div class="dialog-field-row">
        <div>
          <label class="dialog-field-label" for="addViscosityInput">Viscosity (Optional)</label>
          <input type="number" id="addViscosityInput" class="form-control premium-input" placeholder="Enter viscosity" min="0" step="any" />
        </div>
        <div>
          <label class="dialog-field-label" for="addCohesionInput">Cohesion (Optional)</label>
          <input type="number" id="addCohesionInput" class="form-control premium-input" placeholder="Enter cohesion" min="0" step="any" />
        </div>
      </div>

      <div class="dialog-field-row">
        <div>
          <label class="dialog-field-label" for="addShearInput">Shear (Optional)</label>
          <input type="number" id="addShearInput" class="form-control premium-input" placeholder="Enter shear" min="0" step="any" />
        </div>
        <div>
          <label class="dialog-field-label" for="addDensityInput">Density (Optional)</label>
          <input type="number" id="addDensityInput" class="form-control premium-input" placeholder="Enter density" min="0" step="any" />
        </div>
      </div>

      <div id="addAdhesiveError" class="dialog-error"></div>
      <div class="dialog-actions">
        <button class="logout-btn" type="button" onclick="closeAddDialog()">Cancel</button>
        <button class="logout-btn primary" type="button" id="addSubmitBtn" onclick="submitAdd()">Save Adhesive</button>
      </div>
    </div>
  </div>
</div>

<!-- ============ EDIT ADHESIVE DIALOG ============ -->
<div class="logout-modal" id="editDialog" aria-hidden="true">
  <div class="logout-dialog" role="dialog" aria-modal="true" aria-labelledby="editDialogTitle"
    style="width: min(460px, 95vw); padding: 0; border-radius: 14px;">
    <div class="dialog-header" style="border-radius: 14px 14px 0 0;">
      <h2 id="editDialogTitle">Edit Adhesive</h2>
      <button class="dialog-close-btn" type="button" onclick="closeEditDialog()" aria-label="Close">×</button>
    </div>
    <div class="dialog-body" style="border-radius: 0 0 14px 14px;">
      <label class="dialog-field-label" for="editSkuIdDisplay">SKU ID</label>
      <input type="text" id="editSkuIdDisplay" class="form-control premium-input" readonly style="margin-bottom: 16px; background: #f1f5f9;" />

      <div class="dialog-field-row">
        <div>
          <label class="dialog-field-label" for="editVendorSelect">Vendor Name</label>
          <select id="editVendorSelect">
            <option value="" disabled hidden>Select vendor</option>
            <% vendors?.forEach(v => { %>
              <option value="<%= v._id %>"><%= v.vendorName %></option>
            <% }) %>
          </select>
        </div>
        <div>
          <label class="dialog-field-label" for="editTypeSelect">Type</label>
          <select id="editTypeSelect">
            <option value="" disabled hidden>Select type</option>
            <option value="ACRYLIC">ACRYLIC</option>
            <option value="HOTMELT">HOTMELT</option>
            <option value="WATERBASE">WATERBASE</option>
          </select>
        </div>
      </div>

      <label class="dialog-field-label" for="editMakeInput">Make</label>
      <input type="text" id="editMakeInput" class="form-control premium-input" placeholder="Enter make" style="margin-bottom: 16px;" />

      <div class="dialog-field-row">
        <div>
          <label class="dialog-field-label" for="editVendorSkuCodeInput">Vendor SKU Code</label>
          <input type="text" id="editVendorSkuCodeInput" class="form-control premium-input" placeholder="Enter vendor SKU code" />
        </div>
        <div>
          <label class="dialog-field-label" for="editShelfLifeInput">Shelf Life</label>
          <input type="text" id="editShelfLifeInput" class="form-control premium-input" placeholder="e.g. 12 Months" />
        </div>
      </div>

      <div class="dialog-field-row">
        <div>
          <label class="dialog-field-label" for="editViscosityInput">Viscosity (Optional)</label>
          <input type="number" id="editViscosityInput" class="form-control premium-input" placeholder="Enter viscosity" min="0" step="any" />
        </div>
        <div>
          <label class="dialog-field-label" for="editCohesionInput">Cohesion (Optional)</label>
          <input type="number" id="editCohesionInput" class="form-control premium-input" placeholder="Enter cohesion" min="0" step="any" />
        </div>
      </div>

      <div class="dialog-field-row">
        <div>
          <label class="dialog-field-label" for="editShearInput">Shear (Optional)</label>
          <input type="number" id="editShearInput" class="form-control premium-input" placeholder="Enter shear" min="0" step="any" />
        </div>
        <div>
          <label class="dialog-field-label" for="editDensityInput">Density (Optional)</label>
          <input type="number" id="editDensityInput" class="form-control premium-input" placeholder="Enter density" min="0" step="any" />
        </div>
      </div>

      <div id="editAdhesiveError" class="dialog-error"></div>
      <div class="dialog-actions">
        <button class="logout-btn" type="button" onclick="closeEditDialog()">Cancel</button>
        <button class="logout-btn primary" type="button" id="editSubmitBtn" onclick="submitEdit()">Update Adhesive</button>
      </div>
    </div>
  </div>
</div>

<!-- ============ VIEW ADHESIVE DIALOG ============ -->
<div class="logout-modal" id="viewDialog" aria-hidden="true">
  <div class="logout-dialog" role="dialog" aria-modal="true" aria-labelledby="viewDialogTitle"
    style="width: min(460px, 95vw); padding: 0; border-radius: 14px;">
    <div class="dialog-header" style="border-radius: 14px 14px 0 0;">
      <h2 id="viewDialogTitle">Adhesive Details</h2>
      <button class="dialog-close-btn" type="button" onclick="closeViewDialog()" aria-label="Close">×</button>
    </div>
    <div class="dialog-body" style="border-radius: 0 0 14px 14px;">
      <div id="viewBody"></div>
    </div>
  </div>
</div>

<!-- ============ DELETE CONFIRMATION DIALOG ============ -->
<div class="logout-modal" id="deleteDialog" aria-hidden="true">
  <div class="logout-dialog" role="dialog" aria-modal="true" aria-labelledby="deleteDialogTitle"
    style="width: min(400px, 95vw);">
    <h2 class="logout-title" id="deleteDialogTitle">
      <i class="fa-solid fa-triangle-exclamation" style="color: #ef4444; margin-right: 8px;"></i>Delete Adhesive?
    </h2>
    <p class="logout-text">
      Are you sure you want to delete <strong id="deleteSkuId"></strong>? This action cannot be undone.
    </p>
    <div class="logout-actions">
      <button class="logout-btn" type="button" onclick="closeDeleteDialog()">Cancel</button>
      <button
        class="logout-btn"
        type="button"
        id="deleteConfirmBtn"
        onclick="submitDelete()"
        style="background: #ef4444; color: #fff; border-color: #ef4444;">
        Yes, Delete
      </button>
    </div>
  </div>
</div>

<!-- Tabulator JS -->
<script src="https://unpkg.com/tabulator-tables@5.5.2/dist/js/tabulator.min.js"></script>

<script id="adhesives-data" type="application/json"><%- safeJson(adhesives) %></script>
<script id="preview-skuid-data" type="application/json"><%- safeJson(previewSkuId) %></script>
<script>
  const adhesivesData = JSON.parse(document.getElementById("adhesives-data").textContent);
  let previewSkuId = JSON.parse(document.getElementById("preview-skuid-data").textContent);

  let addTypeChoices = null;
  let editTypeChoices = null;
  let addVendorChoices = null;
  let editVendorChoices = null;

  // ---- Table ----
  const table = new Tabulator("#adhesive-master-table", {
    data: adhesivesData,
    height: "100%",
    responsiveLayout: false,
    movableColumns: true,
    placeholder: "No adhesive masters added yet.",
    columns: [
      {
        title: "Actions",
        width: 140,
        hozAlign: "center",
        headerSort: false,
        formatter: function (cell) {
          const row = cell.getRow().getData();
          const wrap = document.createElement("div");
          wrap.style.cssText = "display:flex; gap:5px; justify-content:center;";

          const viewBtn = document.createElement("button");
          viewBtn.className = "loc-btn";
          viewBtn.style.cssText = "padding:3px 8px;border:none;border-radius:4px;cursor:pointer;font-size:12px;color:#fff;background:#044a78;";
          viewBtn.title = "View Adhesive";
          viewBtn.innerHTML = '<i class="fa-solid fa-eye"></i>';
          viewBtn.addEventListener("click", () => openViewDialog(row));

          const editBtn = document.createElement("button");
          editBtn.className = "loc-btn";
          editBtn.style.cssText = "padding:3px 8px;border:none;border-radius:4px;cursor:pointer;font-size:12px;color:#fff;background:#e67e22;";
          editBtn.title = "Edit Adhesive";
          editBtn.innerHTML = '<i class="fa-solid fa-pen-to-square"></i>';
          editBtn.addEventListener("click", () => openEditDialog(row));

          const deleteBtn = document.createElement("button");
          deleteBtn.className = "loc-btn";
          deleteBtn.style.cssText = "padding:3px 8px;border:none;border-radius:4px;cursor:pointer;font-size:12px;color:#fff;background:#d9534f;";
          deleteBtn.title = "Delete Adhesive";
          deleteBtn.innerHTML = '<i class="fa-solid fa-trash"></i>';
          deleteBtn.addEventListener("click", () => openDeleteDialog(row._id, row.skuId));

          wrap.appendChild(viewBtn);
          wrap.appendChild(editBtn);
          wrap.appendChild(deleteBtn);
          return wrap;
        },
      },
      { title: "SKU ID", field: "skuId", headerFilter: "input", headerFilterPlaceholder: "Search", minWidth: 160 },
      { title: "Vendor Name", field: "vendorName", headerFilter: "input", minWidth: 150 },
      { title: "Type", field: "type", headerFilter: "input", minWidth: 110 },
      { title: "Make", field: "make", headerFilter: "input", minWidth: 130, formatter: (cell) => cell.getValue() || "—" },
      { title: "Vendor SKU Code", field: "vendorSkuCode", headerFilter: "input", minWidth: 150 },
      { title: "Shelf Life", field: "shelfLife", minWidth: 110 },
      { title: "Viscosity", field: "viscosity", minWidth: 100, formatter: (cell) => cell.getValue() ?? "—" },
      { title: "Cohesion", field: "cohesion", minWidth: 100, formatter: (cell) => cell.getValue() ?? "—" },
      { title: "Shear", field: "shear", minWidth: 90, formatter: (cell) => cell.getValue() ?? "—" },
      { title: "Density", field: "density", minWidth: 90, formatter: (cell) => cell.getValue() ?? "—" },
    ],
  });

  // ---- Helpers ----
  function openModal(id) {
    const d = document.getElementById(id);
    d.classList.add("show");
    d.setAttribute("aria-hidden", "false");
  }
  function closeModal(id) {
    const d = document.getElementById(id);
    d.classList.remove("show");
    d.setAttribute("aria-hidden", "true");
  }
  function showError(elId, msg) {
    const el = document.getElementById(elId);
    el.textContent = msg;
    el.style.display = "block";
  }
  function hideError(elId) {
    document.getElementById(elId).style.display = "none";
  }
  function setLoading(btnId, loading) {
    const btn = document.getElementById(btnId);
    if (!btn) return;
    btn.disabled = loading;
    btn.style.opacity = loading ? "0.7" : "";
  }

  function detailRow(label, value) {
    return `
      <div style="display:flex; justify-content:space-between; padding:9px 0; border-bottom:1px solid #f1f5f9;">
        <span style="color:#64748b; font-size:13px;">${label}</span>
        <span style="color:#0f172a; font-weight:600; font-size:13px;">${value}</span>
      </div>
    `;
  }

  // ---- VIEW ----
  function openViewDialog(row) {
    document.getElementById("viewBody").innerHTML = [
      detailRow("SKU ID", row.skuId),
      detailRow("Vendor Name", row.vendorName || "—"),
      detailRow("Type", row.type || "—"),
      detailRow("Make", row.make || "—"),
      detailRow("Vendor SKU Code", row.vendorSkuCode || "—"),
      detailRow("Shelf Life", row.shelfLife || "—"),
      detailRow("Viscosity", row.viscosity ?? "—"),
      detailRow("Cohesion", row.cohesion ?? "—"),
      detailRow("Shear", row.shear ?? "—"),
      detailRow("Density", row.density ?? "—"),
    ].join("");
    openModal("viewDialog");
  }
  function closeViewDialog() {
    closeModal("viewDialog");
  }

  ["addDialog", "editDialog", "viewDialog", "deleteDialog"].forEach(id => {
    document.getElementById(id).addEventListener("click", function (e) {
      if (e.target === this) closeModal(id);
    });
  });

  document.addEventListener("keydown", e => {
    if (e.key !== "Escape") return;
    ["addDialog", "editDialog", "viewDialog", "deleteDialog"].forEach(id => {
      if (document.getElementById(id).classList.contains("show")) closeModal(id);
    });
  });

  // ---- ADD ----
  function openAddDialog() {
    document.getElementById("addSkuIdPreview").value = previewSkuId;
    document.getElementById("addMakeInput").value = "";
    document.getElementById("addVendorSkuCodeInput").value = "";
    document.getElementById("addShelfLifeInput").value = "";
    document.getElementById("addViscosityInput").value = "";
    document.getElementById("addCohesionInput").value = "";
    document.getElementById("addShearInput").value = "";
    document.getElementById("addDensityInput").value = "";

    if (addTypeChoices) { addTypeChoices.destroy(); addTypeChoices = null; }
    document.getElementById("addTypeSelect").value = "";
    addTypeChoices = new Choices("#addTypeSelect", { searchEnabled: false, shouldSort: false, itemSelectText: "" });

    if (addVendorChoices) { addVendorChoices.destroy(); addVendorChoices = null; }
    document.getElementById("addVendorSelect").value = "";
    addVendorChoices = new Choices("#addVendorSelect", { searchEnabled: true, shouldSort: false, itemSelectText: "" });

    hideError("addAdhesiveError");
    openModal("addDialog");
  }
  function closeAddDialog() {
    closeModal("addDialog");
  }

  async function submitAdd() {
    const vendorId = document.getElementById("addVendorSelect").value;
    const type = document.getElementById("addTypeSelect").value;
    const vendorSkuCode = document.getElementById("addVendorSkuCodeInput").value.trim();
    const shelfLife = document.getElementById("addShelfLifeInput").value.trim();
    if (!vendorId) return showError("addAdhesiveError", "Vendor Name is required.");
    if (!type) return showError("addAdhesiveError", "Type is required.");
    if (!vendorSkuCode) return showError("addAdhesiveError", "Vendor SKU Code is required.");
    if (!shelfLife) return showError("addAdhesiveError", "Shelf Life is required.");

    hideError("addAdhesiveError");
    setLoading("addSubmitBtn", true);
    try {
      const res = await fetch("/sachiko/form/adhesive", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          vendorId,
          type,
          make: document.getElementById("addMakeInput").value.trim(),
          vendorSkuCode,
          shelfLife,
          viscosity: document.getElementById("addViscosityInput").value,
          cohesion: document.getElementById("addCohesionInput").value,
          shear: document.getElementById("addShearInput").value,
          density: document.getElementById("addDensityInput").value,
        }),
      });
      const data = await res.json();
      if (data.success) {
        window.location.href = data.redirect || "/sachiko/form/adhesive";
      } else {
        showError("addAdhesiveError", data.message || "Failed to add adhesive.");
      }
    } catch {
      showError("addAdhesiveError", "Server error. Please try again.");
    } finally {
      setLoading("addSubmitBtn", false);
    }
  }

  // ---- EDIT ----
  let editId = null;

  function openEditDialog(row) {
    editId = row._id;
    document.getElementById("editSkuIdDisplay").value = row.skuId;
    document.getElementById("editMakeInput").value = row.make || "";
    document.getElementById("editVendorSkuCodeInput").value = row.vendorSkuCode || "";
    document.getElementById("editShelfLifeInput").value = row.shelfLife || "";
    document.getElementById("editViscosityInput").value = row.viscosity ?? "";
    document.getElementById("editCohesionInput").value = row.cohesion ?? "";
    document.getElementById("editShearInput").value = row.shear ?? "";
    document.getElementById("editDensityInput").value = row.density ?? "";

    if (editTypeChoices) { editTypeChoices.destroy(); editTypeChoices = null; }
    document.getElementById("editTypeSelect").value = row.type || "";
    editTypeChoices = new Choices("#editTypeSelect", { searchEnabled: false, shouldSort: false, itemSelectText: "" });

    if (editVendorChoices) { editVendorChoices.destroy(); editVendorChoices = null; }
    document.getElementById("editVendorSelect").value = row.vendorId || "";
    editVendorChoices = new Choices("#editVendorSelect", { searchEnabled: true, shouldSort: false, itemSelectText: "" });

    hideError("editAdhesiveError");
    openModal("editDialog");
  }
  function closeEditDialog() {
    closeModal("editDialog");
    editId = null;
  }

  async function submitEdit() {
    const vendorId = document.getElementById("editVendorSelect").value;
    const type = document.getElementById("editTypeSelect").value;
    const vendorSkuCode = document.getElementById("editVendorSkuCodeInput").value.trim();
    const shelfLife = document.getElementById("editShelfLifeInput").value.trim();
    if (!vendorId) return showError("editAdhesiveError", "Vendor Name is required.");
    if (!type) return showError("editAdhesiveError", "Type is required.");
    if (!vendorSkuCode) return showError("editAdhesiveError", "Vendor SKU Code is required.");
    if (!shelfLife) return showError("editAdhesiveError", "Shelf Life is required.");
    if (!editId) return;

    hideError("editAdhesiveError");
    setLoading("editSubmitBtn", true);
    try {
      const res = await fetch(`/sachiko/api/adhesive/${editId}`, {
        method: "PUT",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          vendorId,
          type,
          make: document.getElementById("editMakeInput").value.trim(),
          vendorSkuCode,
          shelfLife,
          viscosity: document.getElementById("editViscosityInput").value,
          cohesion: document.getElementById("editCohesionInput").value,
          shear: document.getElementById("editShearInput").value,
          density: document.getElementById("editDensityInput").value,
        }),
      });
      const data = await res.json();
      if (data.success) {
        window.location.reload();
      } else {
        showError("editAdhesiveError", data.message || "Failed to update adhesive.");
      }
    } catch {
      showError("editAdhesiveError", "Server error. Please try again.");
    } finally {
      setLoading("editSubmitBtn", false);
    }
  }

  // ---- DELETE ----
  let deleteId = null;

  function openDeleteDialog(id, skuId) {
    deleteId = id;
    document.getElementById("deleteSkuId").textContent = skuId;
    openModal("deleteDialog");
  }
  function closeDeleteDialog() {
    closeModal("deleteDialog");
    deleteId = null;
  }

  async function submitDelete() {
    if (!deleteId) return;
    setLoading("deleteConfirmBtn", true);
    try {
      const res = await fetch(`/sachiko/api/adhesive/${deleteId}`, { method: "DELETE" });
      const data = await res.json();
      if (data.success) {
        window.location.reload();
      } else {
        closeDeleteDialog();
        alert(data.message || "Failed to delete adhesive.");
      }
    } catch {
      closeDeleteDialog();
      alert("Error deleting adhesive.");
    } finally {
      setLoading("deleteConfirmBtn", false);
    }
  }
</script>
